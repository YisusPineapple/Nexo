import 'package:flutter/material.dart';

/// Placeholder for Sub-phase 3.2 — actual content (list, order,
/// search) will be added in Sub-phase 3.3, the first actual screen of
/// LIBRARY.
class SongsScreen extends StatelessWidget {
  const SongsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Songs'));
  }
}